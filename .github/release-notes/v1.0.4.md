## What's new

- **An upload carries on where it stopped.** Close the lid, lose the
  connection, shut the machine down — the parts already uploaded stay
  uploaded, and the next run continues from there instead of starting the file
  again. Before this, nine gigabytes sent and then an interruption meant nine
  gigabytes sent again. **An interruption now costs at most 16 MB of
  re-uploading, whatever the size of the file.**

- **It picks itself up when you sign in.** A backup stopped by a shutdown does
  not wait for the next half-hourly tick; it starts a minute after you next
  log in, on its own. Nothing to run and nothing to click.

- **An interrupted backup says so.** `r2b status` and the folder list now show
  the run that stopped, how far it got, how much of it the bucket already
  holds, and that it resumes by itself. That state used to look identical to
  "nothing has ever run" — the progress was there on disk and nothing read it.

- **Progress survives closing the app.** How far a part-uploaded file got is
  read from the local index, so reopening `r2b` shows it immediately without
  asking the bucket anything.

- **The last five commands have keys.** Everything the command line can do,
  the window can do — and it is checked rather than claimed now. `r2b ls` is
  <kbd>f</kbd>, `restore --machine` is <kbd>c</kbd>, `restore --verify` is a
  field on the restore form, `remove --purge` is <kbd>X</kbd>, and
  `schedule --repair` is <kbd>p</kbd>.

- **See what is in your bucket, from your bucket.** Press <kbd>c</kbd> on
  Folders for every backup stored, this computer's and your other computers',
  and press enter to restore one. This is what a machine that has just signed
  in needs: it has working credentials, an empty folder list, and until now
  nothing on screen naming the data that was already there.

- **What is stored, with sizes.** Press <kbd>f</kbd> on a folder for the
  objects it holds, largest first, and the total. It reads the local index,
  so it costs no operations.

- **Delete a folder's stored copy without leaving the window.** <kbd>X</kbd>
  stops backing a folder up and deletes every object stored for it. It asks
  you to type the folder's name back first, which is the same bar `--purge`
  sets on the command line.

- **Adding your first folder offers to keep backing it up.** The command has
  always ended by asking; the window said nothing, so a folder added there was
  backed up exactly once unless you went and found the Schedule mode. It is
  asked once, after the first backup finishes, and never when backups are
  already scheduled.

- **A prettier sign-in email.** The six-digit code arrives as a designed
  message rather than one line of plain text — and it loads nothing: no
  hosted images, no web font, no tracking pixel. It has a dark version, and
  it says the code's real lifetime rather than a number written into the copy.

## Heads up

- **Files over 16 MB now upload in parts**, where the cut-off used to be
  64 MB. That is what makes them resumable, and it costs a few more requests
  per large file — four instead of one for a 40 MB file, against a free
  allowance of a million a month. Nothing about small files changed.

## Fixed

- **A stalled upload is retried instead of failed.** When a connection stalls
  long enough for the server to stop waiting, it rejects the upload for being
  shorter than promised. That is a transient condition and it now gets another
  attempt; it used to be treated as final, and a busy connection could fail
  dozens of files at once.

- **A failed upload now says what actually stopped it.** "The file was most
  likely changed or truncated while it was being uploaded", with how far it
  got, instead of a protocol complaint about byte counts that named neither
  the cause nor the file's size.

- **An upload that failed once could not be retried.** Retrying means sending
  the file again from the start, and the progress counter wrapped around it
  hid the file's own ability to rewind — so any upload that hit a transient
  failure was simply lost for that run. Nothing was ever silently skipped:
  failures were counted and named, and the next run picked them up. But on a
  flaky connection that is a run that does less than it should have.

- **The window never showed which version it was.** The header had a slot for
  it that was never filled in.

- **The keyboard help was trimmed for a terminal you were not using.** It was
  cut at two fixed widths, so a mode with more to offer lost more of its keys
  regardless of how wide the window actually was. It is measured now, and
  <kbd>tab</kbd> and <kbd>q</kbd> survive every width.
